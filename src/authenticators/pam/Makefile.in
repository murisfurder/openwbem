# Makefile.in generated by automake 1.9.6 from Makefile.am.
# @configure_input@

# Copyright (C) 1994, 1995, 1996, 1997, 1998, 1999, 2000, 2001, 2002,
# 2003, 2004, 2005  Free Software Foundation, Inc.
# This Makefile.in is free software; the Free Software Foundation
# gives unlimited permission to copy and/or distribute it,
# with or without modifications, as long as this notice is preserved.

# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY, to the extent permitted by law; without
# even the implied warranty of MERCHANTABILITY or FITNESS FOR A
# PARTICULAR PURPOSE.

@SET_MAKE@

srcdir = @srcdir@
top_srcdir = @top_srcdir@
VPATH = @srcdir@
pkgdatadir = $(datadir)/@PACKAGE@
pkglibdir = $(libdir)/@PACKAGE@
pkgincludedir = $(includedir)/@PACKAGE@
top_builddir = ../../..
am__cd = CDPATH="$${ZSH_VERSION+.}$(PATH_SEPARATOR)" && cd
INSTALL = @INSTALL@
install_sh_DATA = $(install_sh) -c -m 644
install_sh_PROGRAM = $(install_sh) -c
install_sh_SCRIPT = $(install_sh) -c
INSTALL_HEADER = $(INSTALL_DATA)
transform = $(program_transform_name)
NORMAL_INSTALL = :
PRE_INSTALL = :
POST_INSTALL = :
NORMAL_UNINSTALL = :
PRE_UNINSTALL = :
POST_UNINSTALL = :
build_triplet = @build@
host_triplet = @host@
DIST_COMMON = $(srcdir)/Makefile.am $(srcdir)/Makefile.in \
	$(top_srcdir)/Makefile-shared-lib-inst.inc \
	$(top_srcdir)/Makefile-shared-lib.inc
subdir = src/authenticators/pam
ACLOCAL_M4 = $(top_srcdir)/aclocal.m4
am__aclocal_m4_deps = $(top_srcdir)/acinclude.m4 \
	$(top_srcdir)/configure.in
am__configure_deps = $(am__aclocal_m4_deps) $(CONFIGURE_DEPENDENCIES) \
	$(ACLOCAL_M4)
mkinstalldirs = $(install_sh) -d
CONFIG_HEADER = $(top_builddir)/config.h
CONFIG_CLEAN_FILES =
LIBRARIES = $(noinst_LIBRARIES)
ARFLAGS = cru
libpamauthentication_a_AR = $(AR) $(ARFLAGS)
libpamauthentication_a_LIBADD =
am__libpamauthentication_a_SOURCES_DIST = OW_PAMAuthentication.cpp
@USE_PAM_TRUE@am_libpamauthentication_a_OBJECTS =  \
@USE_PAM_TRUE@	OW_PAMAuthentication.$(OBJEXT)
libpamauthentication_a_OBJECTS = $(am_libpamauthentication_a_OBJECTS)
DEFAULT_INCLUDES = -I. -I$(srcdir) -I$(top_builddir)
depcomp = $(SHELL) $(top_srcdir)/depcomp
am__depfiles_maybe = depfiles
CXXCOMPILE = $(CXX) $(DEFS) $(DEFAULT_INCLUDES) $(INCLUDES) \
	$(AM_CPPFLAGS) $(CPPFLAGS) $(AM_CXXFLAGS) $(CXXFLAGS)
CXXLD = $(CXX)
SOURCES = $(libpamauthentication_a_SOURCES)
DIST_SOURCES = $(am__libpamauthentication_a_SOURCES_DIST)
ETAGS = etags
CTAGS = ctags
DISTFILES = $(DIST_COMMON) $(DIST_SOURCES) $(TEXINFOS) $(EXTRA_DIST)
ACLOCAL = @ACLOCAL@
AIX = @AIX@
AMDEP_FALSE = @AMDEP_FALSE@
AMDEP_TRUE = @AMDEP_TRUE@
AMTAR = @AMTAR@
AR = @AR@
AUTOCONF = @AUTOCONF@
AUTOHEADER = @AUTOHEADER@
AUTOMAKE = @AUTOMAKE@
AWK = @AWK@
BINLINKAPPENDFLAGS = @BINLINKAPPENDFLAGS@
CBINLINKFLAGS = @CBINLINKFLAGS@
CC = @CC@
CCDEPMODE = @CCDEPMODE@
CFLAGS = @CFLAGS@
CLINK = @CLINK@
CPP = @CPP@
CPPFLAGS = @CPPFLAGS@
CURRENT_LINK_MODE_FLAGS = @CURRENT_LINK_MODE_FLAGS@
CXX = @CXX@
CXXBINLINKFLAGS = @CXXBINLINKFLAGS@
CXXBUNDLELIBLINKFLAGS = @CXXBUNDLELIBLINKFLAGS@
CXXCPP = @CXXCPP@
CXXDEPMODE = @CXXDEPMODE@
CXXFLAGS = @CXXFLAGS@
CXXLINK = @CXXLINK@
CXXSHAREDLIBLINKFLAGS = @CXXSHAREDLIBLINKFLAGS@
CYGPATH_W = @CYGPATH_W@
DEFS = @DEFS@
DEPDIR = @DEPDIR@
DISABLE_ASSOCIATION_TRAVERSAL_FALSE = @DISABLE_ASSOCIATION_TRAVERSAL_FALSE@
DISABLE_ASSOCIATION_TRAVERSAL_TRUE = @DISABLE_ASSOCIATION_TRAVERSAL_TRUE@
DISABLE_DIGEST_FALSE = @DISABLE_DIGEST_FALSE@
DISABLE_DIGEST_TRUE = @DISABLE_DIGEST_TRUE@
DISABLE_INSTANCE_MANIPULATION_FALSE = @DISABLE_INSTANCE_MANIPULATION_FALSE@
DISABLE_INSTANCE_MANIPULATION_TRUE = @DISABLE_INSTANCE_MANIPULATION_TRUE@
DISABLE_NAMESPACE_MANIPULATION_FALSE = @DISABLE_NAMESPACE_MANIPULATION_FALSE@
DISABLE_NAMESPACE_MANIPULATION_TRUE = @DISABLE_NAMESPACE_MANIPULATION_TRUE@
DISABLE_PROPERTY_OPERATIONS_FALSE = @DISABLE_PROPERTY_OPERATIONS_FALSE@
DISABLE_PROPERTY_OPERATIONS_TRUE = @DISABLE_PROPERTY_OPERATIONS_TRUE@
DISABLE_QUALIFIER_DECLARATION_FALSE = @DISABLE_QUALIFIER_DECLARATION_FALSE@
DISABLE_QUALIFIER_DECLARATION_TRUE = @DISABLE_QUALIFIER_DECLARATION_TRUE@
DISABLE_SCHEMA_MANIPULATION_FALSE = @DISABLE_SCHEMA_MANIPULATION_FALSE@
DISABLE_SCHEMA_MANIPULATION_TRUE = @DISABLE_SCHEMA_MANIPULATION_TRUE@
DL_LIB = @DL_LIB@
DYNAMIC_EXPORT_FLAG = @DYNAMIC_EXPORT_FLAG@
DYNAMIC_LINK_FLAGS = @DYNAMIC_LINK_FLAGS@
ECHO_C = @ECHO_C@
ECHO_N = @ECHO_N@
ECHO_T = @ECHO_T@
EGREP = @EGREP@
ENABLE_CMPI_FALSE = @ENABLE_CMPI_FALSE@
ENABLE_CMPI_TRUE = @ENABLE_CMPI_TRUE@
ENABLE_DB4_REPOSITORY_FALSE = @ENABLE_DB4_REPOSITORY_FALSE@
ENABLE_DB4_REPOSITORY_TRUE = @ENABLE_DB4_REPOSITORY_TRUE@
EXEEXT = @EXEEXT@
FALSE_PATHNAME = @FALSE_PATHNAME@
HAVE_FLEX_FALSE = @HAVE_FLEX_FALSE@
HAVE_FLEX_TRUE = @HAVE_FLEX_TRUE@
HAVE_PERL_EMBED_FALSE = @HAVE_PERL_EMBED_FALSE@
HAVE_PERL_EMBED_TRUE = @HAVE_PERL_EMBED_TRUE@
HAVE_SLP_FALSE = @HAVE_SLP_FALSE@
HAVE_SLP_TRUE = @HAVE_SLP_TRUE@
INSTALL_DATA = @INSTALL_DATA@
INSTALL_PROGRAM = @INSTALL_PROGRAM@
INSTALL_SCRIPT = @INSTALL_SCRIPT@
INSTALL_STRIP_PROGRAM = @INSTALL_STRIP_PROGRAM@
LDFLAGS = @LDFLAGS@
LEX = @LEX@
LEXLIB = @LEXLIB@
LEX_OUTPUT_ROOT = @LEX_OUTPUT_ROOT@
LIBOBJS = @LIBOBJS@
LIBS = @LIBS@
LIB_EXT = @LIB_EXT@
LTLIBOBJS = @LTLIBOBJS@
MAINT = @MAINT@
MAINTAINER_MODE_FALSE = @MAINTAINER_MODE_FALSE@
MAINTAINER_MODE_TRUE = @MAINTAINER_MODE_TRUE@
MAKEINFO = @MAKEINFO@
MV = @MV@
OBJEXT = @OBJEXT@
OPENWBEM_LIBRARY_VERSION = @OPENWBEM_LIBRARY_VERSION@
OPENWBEM_MAJOR_VERSION = @OPENWBEM_MAJOR_VERSION@
OPENWBEM_MICRO_VERSION = @OPENWBEM_MICRO_VERSION@
OPENWBEM_MINOR_VERSION = @OPENWBEM_MINOR_VERSION@
OPENWBEM_VERSION = @OPENWBEM_VERSION@
OW_NETWARE_INSTALL_FALSE = @OW_NETWARE_INSTALL_FALSE@
OW_NETWARE_INSTALL_TRUE = @OW_NETWARE_INSTALL_TRUE@
OW_STATIC_LINKING_FALSE = @OW_STATIC_LINKING_FALSE@
OW_STATIC_LINKING_TRUE = @OW_STATIC_LINKING_TRUE@
PACKAGE = @PACKAGE@
PACKAGE_BUGREPORT = @PACKAGE_BUGREPORT@
PACKAGE_NAME = @PACKAGE_NAME@
PACKAGE_PREFIX = @PACKAGE_PREFIX@
PACKAGE_PREFIX_D = @PACKAGE_PREFIX_D@
PACKAGE_STRING = @PACKAGE_STRING@
PACKAGE_TARNAME = @PACKAGE_TARNAME@
PACKAGE_VERSION = @PACKAGE_VERSION@
PAM_LIB = @PAM_LIB@
PATH_SEPARATOR = @PATH_SEPARATOR@
PERL = @PERL@
PIDFILE_DIR = @PIDFILE_DIR@
RANLIB = @RANLIB@
RM = @RM@
SED = @SED@
SET_MAKE = @SET_MAKE@
SHELL = @SHELL@
SLP_LIB = @SLP_LIB@
SSL_LIBS = @SSL_LIBS@
STATIC_LINK_FLAGS = @STATIC_LINK_FLAGS@
STATIC_SERVICES = @STATIC_SERVICES@
STRIP = @STRIP@
TRUE_PATHNAME = @TRUE_PATHNAME@
USE_AIX_AUTHENTICATE_FALSE = @USE_AIX_AUTHENTICATE_FALSE@
USE_AIX_AUTHENTICATE_TRUE = @USE_AIX_AUTHENTICATE_TRUE@
USE_PAM_FALSE = @USE_PAM_FALSE@
USE_PAM_TRUE = @USE_PAM_TRUE@
VERSION = @VERSION@
YACC = @YACC@
Z_LIBS = @Z_LIBS@
ac_ct_CC = @ac_ct_CC@
ac_ct_CXX = @ac_ct_CXX@
ac_ct_RANLIB = @ac_ct_RANLIB@
ac_ct_STRIP = @ac_ct_STRIP@
am__fastdepCC_FALSE = @am__fastdepCC_FALSE@
am__fastdepCC_TRUE = @am__fastdepCC_TRUE@
am__fastdepCXX_FALSE = @am__fastdepCXX_FALSE@
am__fastdepCXX_TRUE = @am__fastdepCXX_TRUE@
am__include = @am__include@
am__leading_dot = @am__leading_dot@
am__quote = @am__quote@
am__tar = @am__tar@
am__untar = @am__untar@
bindir = @bindir@
build = @build@
build_alias = @build_alias@
build_cpu = @build_cpu@
build_os = @build_os@
build_vendor = @build_vendor@
datadir = @datadir@
exec_prefix = @exec_prefix@
host = @host@
host_alias = @host_alias@
host_cpu = @host_cpu@
host_os = @host_os@
host_vendor = @host_vendor@
includedir = @includedir@
infodir = @infodir@
install_sh = @install_sh@
libdir = @libdir@
libexecdir = @libexecdir@
localstatedir = @localstatedir@
mandir = @mandir@
mkdir_p = @mkdir_p@
oldincludedir = @oldincludedir@
perl_archlibexp = @perl_archlibexp@
perl_embed_ccflags = @perl_embed_ccflags@
perl_embed_ldflags = @perl_embed_ldflags@
perl_privlibexp = @perl_privlibexp@
perl_useshrplib = @perl_useshrplib@
prefix = @prefix@
program_transform_name = @program_transform_name@
sbindir = @sbindir@
sharedstatedir = @sharedstatedir@
sysconfdir = @sysconfdir@
target_alias = @target_alias@
MAKEBUNDLE = 1
@USE_PAM_TRUE@SO_EXT = $(LIBRARY_BINARY_VERSION)
@USE_PAM_TRUE@SOFILENAME = $@.$(SO_EXT)
@USE_PAM_TRUE@DYFILENAME = $(sort $(patsubst %.dylib,%,$(patsubst %.dylib.bundle,%.dylib,$@))).$(SO_EXT).dylib
@USE_PAM_TRUE@TMPLIBDIR = .$@.libs

# Here's what this does:  It finds all symbols in each object file that are one
# of T,D,B (Global text symbol, Global data symbol, Global BSS symbol), where
# the first character is not a '.', and puts them in a list, which is then
# sorted (unique), and dumped in LIBNAME.exp 
# 
# The nm options are: Berleky style (3 column), no demangling, unsorted, global
# symbols only. 
@USE_PAM_TRUE@CREATE_EXPORTS = nm -BCpg $(TMPLIBDIR)/*.o | \
@USE_PAM_TRUE@			$(AWK) '{ if((($$2 == "T") || \
@USE_PAM_TRUE@				   ($$2 == "D") || \
@USE_PAM_TRUE@				   ($$2 == "B")) && \
@USE_PAM_TRUE@				  (substr($$3,1,1) != ".")) \
@USE_PAM_TRUE@				{ print $$3 } }' | \
@USE_PAM_TRUE@			sort -u > $(@:%.so=%.exp)


# Rules for NetWare

#	Look only for symbols in the text "T" section because that is all the 
#	fake library stuff needs to be concerned with.
@USE_PAM_TRUE@CREATE_IMPORTS = nm -Bpg $(TMPLIBDIR)/*.o | \
@USE_PAM_TRUE@			$(AWK) '{ if( ($$2 == "T") && \
@USE_PAM_TRUE@				      (substr($$3,1,1) != ".") && \
@USE_PAM_TRUE@				      (substr($$3,1,1) != "_") ) \
@USE_PAM_TRUE@				{ print $$3 } }' > $(@:%.nlm=%.imp)

@USE_PAM_TRUE@EXPORT_PREFIX = createAuthenticator
@USE_PAM_TRUE@OW_SHARED_LIBS = libpamauthentication.$(LIB_EXT)
@USE_PAM_TRUE@SHAREDLIBADD = -lpam -L$(top_builddir)/src/common -lopenwbem
# increment this number every time a release breaks binary compatibility
@USE_PAM_TRUE@LIBRARY_BINARY_VERSION = $(OPENWBEM_LIBRARY_VERSION)
@USE_PAM_TRUE@noinst_LIBRARIES = \
@USE_PAM_TRUE@	libpamauthentication.a 


#install to here
@USE_PAM_TRUE@thelibdir = $(libdir)

#create links here
@USE_PAM_TRUE@OW_EXTRA_LINK_DIRS = $(pkglibdir)/authentication
@USE_PAM_TRUE@libpamauthentication_a_SOURCES = \
@USE_PAM_TRUE@OW_PAMAuthentication.cpp

@USE_PAM_TRUE@INCLUDES = \
@USE_PAM_TRUE@-I$(top_srcdir)/src/common	\
@USE_PAM_TRUE@-I$(top_srcdir)/src/cim	\
@USE_PAM_TRUE@-I$(top_srcdir)/src/ifcs

EXTRA_DIST = OW_PAMAuthentication.cpp
all: all-am

.SUFFIXES:
.SUFFIXES: .cpp .o .obj
$(srcdir)/Makefile.in: @MAINTAINER_MODE_TRUE@ $(srcdir)/Makefile.am $(top_srcdir)/Makefile-shared-lib.inc $(top_srcdir)/Makefile-shared-lib-inst.inc $(am__configure_deps)
	@for dep in $?; do \
	  case '$(am__configure_deps)' in \
	    *$$dep*) \
	      cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh \
		&& exit 0; \
	      exit 1;; \
	  esac; \
	done; \
	echo ' cd $(top_srcdir) && $(AUTOMAKE) --foreign  src/authenticators/pam/Makefile'; \
	cd $(top_srcdir) && \
	  $(AUTOMAKE) --foreign  src/authenticators/pam/Makefile
.PRECIOUS: Makefile
Makefile: $(srcdir)/Makefile.in $(top_builddir)/config.status
	@case '$?' in \
	  *config.status*) \
	    cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh;; \
	  *) \
	    echo ' cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__depfiles_maybe)'; \
	    cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__depfiles_maybe);; \
	esac;

$(top_builddir)/config.status: $(top_srcdir)/configure $(CONFIG_STATUS_DEPENDENCIES)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh

$(top_srcdir)/configure: @MAINTAINER_MODE_TRUE@ $(am__configure_deps)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh
$(ACLOCAL_M4): @MAINTAINER_MODE_TRUE@ $(am__aclocal_m4_deps)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh

clean-noinstLIBRARIES:
	-test -z "$(noinst_LIBRARIES)" || rm -f $(noinst_LIBRARIES)
libpamauthentication.a: $(libpamauthentication_a_OBJECTS) $(libpamauthentication_a_DEPENDENCIES) 
	-rm -f libpamauthentication.a
	$(libpamauthentication_a_AR) libpamauthentication.a $(libpamauthentication_a_OBJECTS) $(libpamauthentication_a_LIBADD)
	$(RANLIB) libpamauthentication.a

mostlyclean-compile:
	-rm -f *.$(OBJEXT)

distclean-compile:
	-rm -f *.tab.c

@AMDEP_TRUE@@am__include@ @am__quote@./$(DEPDIR)/OW_PAMAuthentication.Po@am__quote@

.cpp.o:
@am__fastdepCXX_TRUE@	if $(CXXCOMPILE) -MT $@ -MD -MP -MF "$(DEPDIR)/$*.Tpo" -c -o $@ $<; \
@am__fastdepCXX_TRUE@	then mv -f "$(DEPDIR)/$*.Tpo" "$(DEPDIR)/$*.Po"; else rm -f "$(DEPDIR)/$*.Tpo"; exit 1; fi
@AMDEP_TRUE@@am__fastdepCXX_FALSE@	source='$<' object='$@' libtool=no @AMDEPBACKSLASH@
@AMDEP_TRUE@@am__fastdepCXX_FALSE@	DEPDIR=$(DEPDIR) $(CXXDEPMODE) $(depcomp) @AMDEPBACKSLASH@
@am__fastdepCXX_FALSE@	$(CXXCOMPILE) -c -o $@ $<

.cpp.obj:
@am__fastdepCXX_TRUE@	if $(CXXCOMPILE) -MT $@ -MD -MP -MF "$(DEPDIR)/$*.Tpo" -c -o $@ `$(CYGPATH_W) '$<'`; \
@am__fastdepCXX_TRUE@	then mv -f "$(DEPDIR)/$*.Tpo" "$(DEPDIR)/$*.Po"; else rm -f "$(DEPDIR)/$*.Tpo"; exit 1; fi
@AMDEP_TRUE@@am__fastdepCXX_FALSE@	source='$<' object='$@' libtool=no @AMDEPBACKSLASH@
@AMDEP_TRUE@@am__fastdepCXX_FALSE@	DEPDIR=$(DEPDIR) $(CXXDEPMODE) $(depcomp) @AMDEPBACKSLASH@
@am__fastdepCXX_FALSE@	$(CXXCOMPILE) -c -o $@ `$(CYGPATH_W) '$<'`
uninstall-info-am:

ID: $(HEADERS) $(SOURCES) $(LISP) $(TAGS_FILES)
	list='$(SOURCES) $(HEADERS) $(LISP) $(TAGS_FILES)'; \
	unique=`for i in $$list; do \
	    if test -f "$$i"; then echo $$i; else echo $(srcdir)/$$i; fi; \
	  done | \
	  $(AWK) '    { files[$$0] = 1; } \
	       END { for (i in files) print i; }'`; \
	mkid -fID $$unique
tags: TAGS

TAGS:  $(HEADERS) $(SOURCES)  $(TAGS_DEPENDENCIES) \
		$(TAGS_FILES) $(LISP)
	tags=; \
	here=`pwd`; \
	list='$(SOURCES) $(HEADERS)  $(LISP) $(TAGS_FILES)'; \
	unique=`for i in $$list; do \
	    if test -f "$$i"; then echo $$i; else echo $(srcdir)/$$i; fi; \
	  done | \
	  $(AWK) '    { files[$$0] = 1; } \
	       END { for (i in files) print i; }'`; \
	if test -z "$(ETAGS_ARGS)$$tags$$unique"; then :; else \
	  test -n "$$unique" || unique=$$empty_fix; \
	  $(ETAGS) $(ETAGSFLAGS) $(AM_ETAGSFLAGS) $(ETAGS_ARGS) \
	    $$tags $$unique; \
	fi
ctags: CTAGS
CTAGS:  $(HEADERS) $(SOURCES)  $(TAGS_DEPENDENCIES) \
		$(TAGS_FILES) $(LISP)
	tags=; \
	here=`pwd`; \
	list='$(SOURCES) $(HEADERS)  $(LISP) $(TAGS_FILES)'; \
	unique=`for i in $$list; do \
	    if test -f "$$i"; then echo $$i; else echo $(srcdir)/$$i; fi; \
	  done | \
	  $(AWK) '    { files[$$0] = 1; } \
	       END { for (i in files) print i; }'`; \
	test -z "$(CTAGS_ARGS)$$tags$$unique" \
	  || $(CTAGS) $(CTAGSFLAGS) $(AM_CTAGSFLAGS) $(CTAGS_ARGS) \
	     $$tags $$unique

GTAGS:
	here=`$(am__cd) $(top_builddir) && pwd` \
	  && cd $(top_srcdir) \
	  && gtags -i $(GTAGS_ARGS) $$here

distclean-tags:
	-rm -f TAGS ID GTAGS GRTAGS GSYMS GPATH tags

distdir: $(DISTFILES)
	$(mkdir_p) $(distdir)/../../..
	@srcdirstrip=`echo "$(srcdir)" | sed 's|.|.|g'`; \
	topsrcdirstrip=`echo "$(top_srcdir)" | sed 's|.|.|g'`; \
	list='$(DISTFILES)'; for file in $$list; do \
	  case $$file in \
	    $(srcdir)/*) file=`echo "$$file" | sed "s|^$$srcdirstrip/||"`;; \
	    $(top_srcdir)/*) file=`echo "$$file" | sed "s|^$$topsrcdirstrip/|$(top_builddir)/|"`;; \
	  esac; \
	  if test -f $$file || test -d $$file; then d=.; else d=$(srcdir); fi; \
	  dir=`echo "$$file" | sed -e 's,/[^/]*$$,,'`; \
	  if test "$$dir" != "$$file" && test "$$dir" != "."; then \
	    dir="/$$dir"; \
	    $(mkdir_p) "$(distdir)$$dir"; \
	  else \
	    dir=''; \
	  fi; \
	  if test -d $$d/$$file; then \
	    if test -d $(srcdir)/$$file && test $$d != $(srcdir); then \
	      cp -pR $(srcdir)/$$file $(distdir)$$dir || exit 1; \
	    fi; \
	    cp -pR $$d/$$file $(distdir)$$dir || exit 1; \
	  else \
	    test -f $(distdir)/$$file \
	    || cp -p $$d/$$file $(distdir)/$$file \
	    || exit 1; \
	  fi; \
	done
check-am: all-am
check: check-am
@USE_PAM_FALSE@all-local:
all-am: Makefile $(LIBRARIES) all-local
installdirs:
install: install-am
install-exec: install-exec-am
install-data: install-data-am
uninstall: uninstall-am

install-am: all-am
	@$(MAKE) $(AM_MAKEFLAGS) install-exec-am install-data-am

installcheck: installcheck-am
install-strip:
	$(MAKE) $(AM_MAKEFLAGS) INSTALL_PROGRAM="$(INSTALL_STRIP_PROGRAM)" \
	  install_sh_PROGRAM="$(INSTALL_STRIP_PROGRAM)" INSTALL_STRIP_FLAG=-s \
	  `test -z '$(STRIP)' || \
	    echo "INSTALL_PROGRAM_ENV=STRIPPROG='$(STRIP)'"` install
mostlyclean-generic:

clean-generic:

distclean-generic:
	-test -z "$(CONFIG_CLEAN_FILES)" || rm -f $(CONFIG_CLEAN_FILES)

maintainer-clean-generic:
	@echo "This command is intended for maintainers to use"
	@echo "it deletes files that may require special tools to rebuild."
@USE_PAM_FALSE@uninstall-local:
@USE_PAM_FALSE@install-exec-local:
clean: clean-am

clean-am: clean-generic clean-local clean-noinstLIBRARIES \
	mostlyclean-am

distclean: distclean-am
	-rm -rf ./$(DEPDIR)
	-rm -f Makefile
distclean-am: clean-am distclean-compile distclean-generic \
	distclean-tags

dvi: dvi-am

dvi-am:

html: html-am

info: info-am

info-am:

install-data-am:

install-exec-am: install-exec-local

install-info: install-info-am

install-man:

installcheck-am:

maintainer-clean: maintainer-clean-am
	-rm -rf ./$(DEPDIR)
	-rm -f Makefile
maintainer-clean-am: distclean-am maintainer-clean-generic

mostlyclean: mostlyclean-am

mostlyclean-am: mostlyclean-compile mostlyclean-generic

pdf: pdf-am

pdf-am:

ps: ps-am

ps-am:

uninstall-am: uninstall-info-am uninstall-local

.PHONY: CTAGS GTAGS all all-am all-local check check-am clean \
	clean-generic clean-local clean-noinstLIBRARIES ctags \
	distclean distclean-compile distclean-generic distclean-tags \
	distdir dvi dvi-am html html-am info info-am install \
	install-am install-data install-data-am install-exec \
	install-exec-am install-exec-local install-info \
	install-info-am install-man install-strip installcheck \
	installcheck-am installdirs maintainer-clean \
	maintainer-clean-generic mostlyclean mostlyclean-compile \
	mostlyclean-generic pdf pdf-am ps ps-am tags uninstall \
	uninstall-am uninstall-info-am uninstall-local


@USE_PAM_TRUE@%.so %.sl : %.a
@USE_PAM_TRUE@	rm -rf $(TMPLIBDIR)
@USE_PAM_TRUE@	mkdir $(TMPLIBDIR)
@USE_PAM_TRUE@	cd $(TMPLIBDIR) && \
@USE_PAM_TRUE@	$(AR) x ../$< 
@USE_PAM_TRUE@	if [ "x$(AIX)" = "x1" ]; then $(CREATE_EXPORTS); fi
@USE_PAM_TRUE@	$(CXXLINK) $(CXXSHAREDLIBLINKFLAGS) -o $(SOFILENAME) $(TMPLIBDIR)/*.o $(SHAREDLIBADD)
@USE_PAM_TRUE@	rm -rf $@ $(TMPLIBDIR) $(@:%.so=%.exp)
@USE_PAM_TRUE@	ln -s $(SOFILENAME) $@
@USE_PAM_TRUE@	chmod +x $(SOFILENAME)

#	Removed the following line
#	$(CXXLINK) $(CXXSHAREDLIBLINKFLAGS) -o $@ $(TMPLIBDIR)/*.o $(SHAREDLIBADD)
#	and changed it to a 'touch' command because we no longer need dynamic
#	libraries.  The 'touch' command speeds up the compile significantly
@USE_PAM_TRUE@%.nlm : %.a
@USE_PAM_TRUE@	rm -rf $(TMPLIBDIR)
@USE_PAM_TRUE@	mkdir $(TMPLIBDIR)
@USE_PAM_TRUE@	cd $(TMPLIBDIR) && \
@USE_PAM_TRUE@	$(AR) x ../$<
@USE_PAM_TRUE@	$(CREATE_IMPORTS)
@USE_PAM_TRUE@	touch $@
@USE_PAM_TRUE@	cp $@ $(SOFILENAME)
@USE_PAM_TRUE@	rm -rf $(TMPLIBDIR)

# Rules for Darwin so we can get the plugable modules built
# at the same time. Requires that either MAKEDYLIB or MAKEBUNDLE
# be inserted into Makefile.am in the appropriate directory.
# In the case of the four libraries that have to be both loadable
# and linkable, we FIXME EDGE

@USE_PAM_TRUE@%.dylib.bundle %.dylib : %.a
@USE_PAM_TRUE@	rm -f $(@:%.dylib.bundle=%.dylib) $@
@USE_PAM_TRUE@	rm -rf $(TMPLIBDIR)
@USE_PAM_TRUE@	mkdir $(TMPLIBDIR)
@USE_PAM_TRUE@	cd $(TMPLIBDIR) && \
@USE_PAM_TRUE@	$(AR) x ../$<
@USE_PAM_TRUE@	if [ "x$(MAKEDYLIB)" = "x1" ]; then \
@USE_PAM_TRUE@		echo $(CXXLINK) $(CXXSHAREDLIBLINKFLAGS) -o $(DYFILENAME) $(TMPLIBDIR)/*.o $(SHAREDLIBADD); \
@USE_PAM_TRUE@		$(CXXLINK) $(CXXSHAREDLIBLINKFLAGS) -o $(DYFILENAME) $(TMPLIBDIR)/*.o $(SHAREDLIBADD); \
@USE_PAM_TRUE@		echo ln -v -s $(DYFILENAME) $(@:%.dylib.bundle=%.dylib); \
@USE_PAM_TRUE@		ln -v -s $(DYFILENAME) $(@:%.dylib.bundle=%.dylib); \
@USE_PAM_TRUE@	fi;
@USE_PAM_TRUE@	if [ "x$(MAKEBUNDLE)" = "x1" ]; then \
@USE_PAM_TRUE@		echo $(CXXLINK) $(CXXBUNDLELIBLINKFLAGS) -o $(DYFILENAME).bundle $(TMPLIBDIR)/*.o $(SHAREDLIBADD); \
@USE_PAM_TRUE@		$(CXXLINK) $(CXXBUNDLELIBLINKFLAGS) -o $(DYFILENAME).bundle $(TMPLIBDIR)/*.o $(SHAREDLIBADD); \
@USE_PAM_TRUE@		echo ln -v -s $(DYFILENAME) $(@:%.dylib=%.dylib.bundle); \
@USE_PAM_TRUE@		ln -v -s $(DYFILENAME) $(@:%.dylib=%.dylib.bundle); \
@USE_PAM_TRUE@	fi;
@USE_PAM_TRUE@	@echo "$@ done"

@USE_PAM_TRUE@all-ow-shared-libs: $(OW_SHARED_LIBS)

@USE_PAM_TRUE@clean-ow-shared-libs:
@USE_PAM_TRUE@	for i in $(OW_SHARED_LIBS); do \
@USE_PAM_TRUE@		rm -rf $$i* .$$i.libs; \
@USE_PAM_TRUE@	done
@USE_PAM_TRUE@all-local : all-ow-shared-libs
@USE_PAM_TRUE@clean-local : clean-ow-shared-libs

# This is necessary so that it can be used as a variable in the following script. If make expands it, non-bash shells puke on "for linkdir in ;"
@USE_PAM_TRUE@export OW_EXTRA_LINK_DIRS

@USE_PAM_TRUE@install-ow-shared-libs:
@USE_PAM_TRUE@	$(INSTALL_PROGRAM) -d $(DESTDIR)$(thelibdir)
@USE_PAM_TRUE@	for i in $(OW_SHARED_LIBS); do \
@USE_PAM_TRUE@		if [ "x$(STATIC_SERVICES)" = "x" ]; then \
@USE_PAM_TRUE@			if [ "$(LIB_EXT)" = "dylib.bundle" ]; then \
@USE_PAM_TRUE@				LONG_FILE_NAME=`echo $$i|cut -f1 -d'.'`.$(SO_EXT).$(LIB_EXT:%.bundle=%); \
@USE_PAM_TRUE@				SHORT_FILE_NAME=`echo $$i|cut -f1 -d'.'`.$(LIB_EXT:%.bundle=%); \
@USE_PAM_TRUE@				if [ "x$(MAKEDYLIB)" = "x1" ]; then \
@USE_PAM_TRUE@					$(INSTALL_PROGRAM) $$LONG_FILE_NAME $(DESTDIR)$(thelibdir); \
@USE_PAM_TRUE@					rm -f $(DESTDIR)$(thelibdir)/$$SHORT_FILE_NAME; \
@USE_PAM_TRUE@					ln -sf $$LONG_FILE_NAME $(DESTDIR)$(thelibdir)/$$SHORT_FILE_NAME; \
@USE_PAM_TRUE@				fi; \
@USE_PAM_TRUE@				if [ "x$(MAKEBUNDLE)" = "x1" ]; then \
@USE_PAM_TRUE@					$(INSTALL_PROGRAM) $$LONG_FILE_NAME.bundle $(DESTDIR)$(thelibdir); \
@USE_PAM_TRUE@					rm -f $(DESTDIR)$(thelibdir)/$$SHORT_FILE_NAME.bundle; \
@USE_PAM_TRUE@					ln -sf $$LONG_FILE_NAME.bundle $(DESTDIR)$(thelibdir)/$$SHORT_FILE_NAME.bundle; \
@USE_PAM_TRUE@				fi; \
@USE_PAM_TRUE@				for linkdir in $$OW_EXTRA_LINK_DIRS; do \
@USE_PAM_TRUE@					$(INSTALL_PROGRAM) -d $(DESTDIR)$$linkdir; \
@USE_PAM_TRUE@					if [ "x$(MAKEBUNDLE)" = "x1" ]; then \
@USE_PAM_TRUE@						ln -sf $(thelibdir)/$$SHORT_FILE_NAME.bundle $(DESTDIR)$$linkdir; \
@USE_PAM_TRUE@					fi; \
@USE_PAM_TRUE@				done; \
@USE_PAM_TRUE@			elif [ "$(LIB_EXT)" = "nlm" ]; then \
@USE_PAM_TRUE@				$(INSTALL_PROGRAM)  $$i $(DESTDIR)$(thelibdir); \
@USE_PAM_TRUE@				for linkdir in $$OW_EXTRA_LINK_DIRS; do \
@USE_PAM_TRUE@					$(INSTALL_PROGRAM) -d $(DESTDIR)$$linkdir; \
@USE_PAM_TRUE@					$(INSTALL_PROGRAM) $$i $(DESTDIR)$$linkdir; \
@USE_PAM_TRUE@				done; \
@USE_PAM_TRUE@			else \
@USE_PAM_TRUE@				$(INSTALL_PROGRAM)  $$i.$(SO_EXT) $(DESTDIR)$(thelibdir); \
@USE_PAM_TRUE@				rm -f $(DESTDIR)$(thelibdir)/$$i; \
@USE_PAM_TRUE@				ln -sf $$i.$(SO_EXT) $(DESTDIR)$(thelibdir)/$$i; \
@USE_PAM_TRUE@				for linkdir in $$OW_EXTRA_LINK_DIRS; do \
@USE_PAM_TRUE@					$(INSTALL_PROGRAM) -d $(DESTDIR)$$linkdir; \
@USE_PAM_TRUE@					ln -sf $(thelibdir)/$$i $(DESTDIR)$$linkdir; \
@USE_PAM_TRUE@				done; \
@USE_PAM_TRUE@			fi; \
@USE_PAM_TRUE@		else \
@USE_PAM_TRUE@			$(SHELL) $(top_srcdir)/fake_library_creator.sh $(EXPORT_PREFIX) $$i; \
@USE_PAM_TRUE@			if [ -e `echo $$i | cut -f1 -d'.'`.fsl ]; then \
@USE_PAM_TRUE@				$(INSTALL_PROGRAM) `echo $$i | cut -f1 -d'.'`.fsl $(DESTDIR)$(thelibdir)/$$i; \
@USE_PAM_TRUE@				for linkdir in $$OW_EXTRA_LINK_DIRS; do \
@USE_PAM_TRUE@					$(INSTALL_PROGRAM) `echo $$i | cut -f1 -d'.'`.fsl $(DESTDIR)$$linkdir/$$i; \
@USE_PAM_TRUE@				done; \
@USE_PAM_TRUE@				rm -f `echo $$i | cut -f1 -d'.'`.fsl; \
@USE_PAM_TRUE@				if [ -e $(DESTDIR)$(libdir)/libopenwbem_all.a ]; then \
@USE_PAM_TRUE@					echo "Adding the static version of $$i to $(DESTDIR)$(libdir)/libopenwbem_all.a"; \
@USE_PAM_TRUE@					ar r $(DESTDIR)$(libdir)/libopenwbem_all.a `echo $$i | cut -f1 -d'.'`.a; \
@USE_PAM_TRUE@				fi; \
@USE_PAM_TRUE@			else \
@USE_PAM_TRUE@				echo "Not installing $$i because no exports were found."; \
@USE_PAM_TRUE@			fi; \
@USE_PAM_TRUE@		fi; \
@USE_PAM_TRUE@	done;

@USE_PAM_TRUE@uninstall-ow-shared-libs:
@USE_PAM_TRUE@	for i in $(OW_SHARED_LIBS); do \
@USE_PAM_TRUE@		if [ "$(LIB_EXT)" = "dylib.bundle" ]; then \
@USE_PAM_TRUE@			LONG_FILE_NAME=`echo $$i|cut -f1 -d'.'`.$(SO_EXT).$(LIB_EXT:%.bundle=%); \
@USE_PAM_TRUE@			SHORT_FILE_NAME=`echo $$i|cut -f1 -d'.'`.$(LIB_EXT:%.bundle=%); \
@USE_PAM_TRUE@			rm -f $(DESTDIR)$(thelibdir)/$$LONG_FILE_NAME; \
@USE_PAM_TRUE@			rm -f $(DESTDIR)$(thelibdir)/$$LONG_FILE_NAME.bundle; \
@USE_PAM_TRUE@			rm -f $(DESTDIR)$(thelibdir)/$$SHORT_FILE_NAME; \
@USE_PAM_TRUE@			rm -f $(DESTDIR)$(thelibdir)/$$SHORT_FILE_NAME.bundle; \
@USE_PAM_TRUE@			for linkdir in $$OW_EXTRA_LINK_DIRS; do \
@USE_PAM_TRUE@				rm -f $(DESTDIR)$$linkdir/$$SHORT_FILE_NAME; \
@USE_PAM_TRUE@				rm -f $(DESTDIR)$$linkdir/$$SHORT_FILE_NAME.bundle; \
@USE_PAM_TRUE@			done; \
@USE_PAM_TRUE@		elif [ "$(LIB_EXT)" = "nlm" ]; then \
@USE_PAM_TRUE@			rm -f $(DESTDIR)$(thelibdir)/$$i; \
@USE_PAM_TRUE@			for linkdir in $$OW_EXTRA_LINK_DIRS; do \
@USE_PAM_TRUE@				rm -f $(DESTDIR)$$linkdir/$$i; \
@USE_PAM_TRUE@			done; \
@USE_PAM_TRUE@		else \
@USE_PAM_TRUE@			rm -f $(DESTDIR)$(thelibdir)/$$i.$(SO_EXT); \
@USE_PAM_TRUE@			rm -f $(DESTDIR)$(thelibdir)/$$i; \
@USE_PAM_TRUE@			for linkdir in $$OW_EXTRA_LINK_DIRS; do \
@USE_PAM_TRUE@				rm -f $(DESTDIR)$$linkdir/$$i; \
@USE_PAM_TRUE@			done; \
@USE_PAM_TRUE@		fi; \
@USE_PAM_TRUE@	done;

@USE_PAM_TRUE@install-exec-local : install-ow-shared-libs
@USE_PAM_TRUE@uninstall-local : uninstall-ow-shared-libs

@USE_PAM_FALSE@clean-local:
# Tell versions [3.59,3.63) of GNU make to not export all variables.
# Otherwise a system limit (for SysV at least) may be exceeded.
.NOEXPORT:
